# Third-party deps for the firmware/flasher/*.py CH341A tooling.
# Pinned from the working venv so a fresh checkout is reproducible; Dependabot proposes bumps.
#   python -m venv ~/.flasher-venv && ~/.flasher-venv/bin/pip install -r firmware/flasher/requirements.txt
# The CH341A needs no udev rule on a stock Arch/CachyOS install; plain user access works.
pyusb==1.3.1   # ch341common.py transport (NOT flashrom)
