# Copy to secrets.yaml (gitignored) and fill in your own values.
wifi_ssid: "example-ssid"
wifi_password: "example-password"

# API encryption key. ESPHome's dashboard generates one per device when it adopts the node;
# this placeholder only exists so a fresh clone can validate and build. Replace it, or let the
# dashboard write its own, before pairing with Home Assistant.
#   python3 -c "import base64,os; print(base64.b64encode(os.urandom(32)).decode())"
hisense_ac__encryption_key: "kTdsi4OYnRFhUByo4Qh4OcWOevVBJ+donWVN6PeO/GM="
